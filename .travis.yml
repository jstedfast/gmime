language: c
compiler:
  - gcc
sudo: required
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y gtk-doc gpgme-dev
script:
  - ./autogen.sh
  - ./configure --disable-mono && make && make check
